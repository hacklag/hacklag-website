import React from 'react';
// import Router from 'react-router-old';

export default React.createClass({
  displayName: 'Test',
  render() {
    return (
      <div>
        This is only test
      </div>
    );
  }
});
